---
import { Card, CardGrid } from '@astrojs/starlight/components';
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import Footer from '../components/Footer.astro';
import fs from 'node:fs';
import path from 'node:path';

// Read model data from JSON (generated by sync_models.py via GitHub Actions)
let topModels: { display_name: string; author: string; likes: string; downloads: string; slug: string; pipeline_tag: string }[] = [];
try {
  const modelsPath = path.resolve(process.cwd(), 'data/models.json');
  const modelsData = JSON.parse(fs.readFileSync(modelsPath, 'utf-8'));
  // Sort by likes (descending) and take top 6
  topModels = modelsData
    .sort((a: any, b: any) => {
      const likesA = parseInt(String(a.likes).replace(/[^\d]/g, '')) || 0;
      const likesB = parseInt(String(b.likes).replace(/[^\d]/g, '')) || 0;
      return likesB - likesA;
    })
    .slice(0, 6);
} catch (e) {
  // If file not found (e.g., first build), use empty array
  console.warn('models.json not found, using empty model list for homepage.');
}

const frontmatter = {
  title: 'SLM Hub',
  description: 'Developer-centric documentation for Small Language Models (SLMs). Learn, choose, and deploy locally or at the edge.',
  template: 'splash',
  prev: false,
  next: false,
};
---

<StarlightPage frontmatter={frontmatter} headings={[]}>
  <div class="sl-container homepage">
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-content">
        <h1 class="hero-title">
          Small Models. <span class="gradient-text">Big Impact.</span>
        </h1>
        <p class="hero-tagline">
          The open-source learning platform for Small Language Models. 
          <strong>100% executable</strong> — every guide runs in Google Colab.
        </p>
        <div class="hero-actions">
          <a href="/slmhub/docs/start-here/" class="sl-link-button" data-variant="primary">
            <span>Start Learning →</span>
          </a>
          <a href="/slmhub/docs/models/generated/directory/" class="sl-link-button" data-variant="minimal">
            <span>Browse Models</span>
          </a>
        </div>
      </div>
      <div class="hero-image">
        <img src="/slmhub/logo.svg" alt="SLM Hub Logo" />
      </div>
    </section>

    <!-- Courses Section - Redesigned with Cards -->
    <section class="courses-section">
      <h2 class="section-title">📚 Courses</h2>
      <p class="section-subtitle">Everything you need to master Small Language Models — from first principles to production deployment.</p>

      <div class="courses-grid">
        <!-- Fundamentals Card -->
        <div class="course-card card-orange">
          <div class="card-header">
            <span class="card-icon">🧠</span>
            <h3>Fundamentals</h3>
          </div>
          <p class="card-desc">Core concepts every SLM developer needs to know</p>
          <ul class="course-list">
            <li><a href="/slmhub/docs/start-here/">Start Here: Introduction to SLMs</a></li>
            <li><a href="/slmhub/docs/learn/foundations/01-neural-networks-basics/">What is AI? (ML, DL, Neural Networks)</a></li>
            <li><a href="/slmhub/docs/learn/foundations/02-transformer-architecture/">Transformers & Attention</a></li>
            <li><a href="/slmhub/docs/learn/fundamentals/tokenization/">Tokenization (BPE, Subword)</a></li>
            <li><a href="/slmhub/docs/learn/fundamentals/embeddings/">Embeddings & Vectors</a></li>
            <li><a href="/slmhub/docs/learn/fundamentals/quantization/">Quantization (4/8-bit, GGUF)</a></li>
          </ul>
          <a href="/slmhub/docs/learn/foundations/" class="card-link">View All Fundamentals →</a>
        </div>

        <!-- Concepts & Techniques Card -->
        <div class="course-card card-purple">
          <div class="card-header">
            <span class="card-icon">💡</span>
            <h3>Concepts & Techniques</h3>
          </div>
          <p class="card-desc">Advanced patterns for building SLM applications</p>
          <ul class="course-list">
            <li><a href="/slmhub/docs/learn/concepts/rag/">RAG: Retrieval-Augmented Generation</a></li>
            <li><a href="/slmhub/docs/learn/concepts/function-calling/">Function Calling & Tool Use</a></li>
            <li><a href="/slmhub/docs/learn/concepts/agents/">Building AI Agents (ReAct)</a></li>
            <li><a href="/slmhub/docs/learn/fundamentals/fine-tuning/">Fine-Tuning (LoRA, QLoRA)</a></li>
            <li><a href="/slmhub/docs/learn/fundamentals/prompting-for-slms/">Prompting Strategies</a></li>
          </ul>
          <a href="/slmhub/docs/learn/advanced-topics/" class="card-link">Explore Concepts →</a>
        </div>

        <!-- Deployment Card -->
        <div class="course-card card-teal">
          <div class="card-header">
            <span class="card-icon">🚀</span>
            <h3>Deployment</h3>
          </div>
          <p class="card-desc">Run models locally, in the cloud, or at the edge</p>
          <ul class="course-list">
            <li><a href="/slmhub/docs/deploy/quickstarts/ollama/">Ollama Quickstart (Local)</a></li>
            <li><a href="/slmhub/docs/deploy/quickstarts/vllm/">vLLM (High-Throughput)</a></li>
            <li><a href="/slmhub/docs/deploy/quickstarts/llama-cpp/">llama.cpp (CPU/Edge)</a></li>
            <li><a href="/slmhub/docs/deploy/quickstarts/transformers-js/">Transformers.js (Browser)</a></li>
            <li><a href="/slmhub/docs/learn/deployment/01-serving-infrastructure/">Serving Infrastructure</a></li>
          </ul>
          <a href="/slmhub/docs/deploy/" class="card-link">All Quickstarts →</a>
        </div>

        <!-- Tools & Resources Card -->
        <div class="course-card card-blue">
          <div class="card-header">
            <span class="card-icon">🛠️</span>
            <h3>Tools & Resources</h3>
          </div>
          <p class="card-desc">Interactive tools and reference materials</p>
          <ul class="course-list">
            <li><a href="/slmhub/docs/tools/model-picker/">Interactive Model Picker</a></li>
            <li><a href="/slmhub/docs/tools/hardware-picker/">Hardware Calculator (VRAM)</a></li>
            <li><a href="/slmhub/docs/tools/resources/">Libraries & Frameworks</a></li>
            <li><a href="/slmhub/docs/models/generated/directory/">Model Directory</a></li>
          </ul>
          <a href="/slmhub/docs/tools/" class="card-link">All Tools →</a>
        </div>

        <!-- Projects Card -->
        <div class="course-card card-gradient">
          <div class="card-header">
            <span class="card-icon">🎯</span>
            <h3>Hands-On Projects</h3>
          </div>
          <p class="card-desc">Build real applications with SLMs</p>
          <ul class="course-list">
            <li><a href="/slmhub/docs/learn/projects/01-code-assistant/">Build a Code Assistant</a></li>
            <li><a href="/slmhub/docs/learn/projects/02-personal-knowledge-base/">Personal Knowledge Base (RAG)</a></li>
            <li><a href="/slmhub/docs/learn/projects/03-function-calling-agent/">Function-Calling Agent</a></li>
          </ul>
          <a href="/slmhub/docs/learn/projects/" class="card-link">Start Building →</a>
        </div>

        <!-- Advanced Topics Card -->
        <div class="course-card card-dark">
          <div class="card-header">
            <span class="card-icon">🔬</span>
            <h3>Advanced & Cutting-Edge</h3>
          </div>
          <p class="card-desc">Latest research and optimization techniques</p>
          <ul class="course-list">
            <li><a href="/slmhub/docs/learn/cutting-edge/01-bitnet-quantization/">BitNet (1.58-bit)</a></li>
            <li><a href="/slmhub/docs/learn/cutting-edge/03-test-time-compute-scaling/">Test-Time Compute</a></li>
            <li><a href="/slmhub/docs/learn/cutting-edge/04-long-context-techniques/">Long Context Techniques</a></li>
            <li><a href="/slmhub/docs/learn/advanced-architectures/">Advanced Architectures</a></li>
          </ul>
          <a href="/slmhub/docs/learn/cutting-edge/" class="card-link">Explore Research →</a>
        </div>
      </div>
    </section>

    <!-- Top Models Section - Table Format -->
    <section class="models-section">
      <h2 class="section-title">🏆 Popular Models</h2>
      <p class="section-subtitle">Auto-synced from Hugging Face daily. Sorted by community popularity.</p>
      
      {topModels.length > 0 ? (
        <div class="models-table-wrapper">
          <table class="models-table">
            <thead>
              <tr>
                <th>Model</th>
                <th>Author</th>
                <th>Task</th>
                <th>Likes</th>
                <th>Downloads</th>
              </tr>
            </thead>
            <tbody>
              {topModels.map((m) => (
                <tr>
                  <td class="model-name-cell">
                    <a href={`/slmhub/docs/models/generated/${m.slug}/`}>{m.display_name}</a>
                  </td>
                  <td class="model-author-cell">{m.author}</td>
                  <td class="model-task-cell">
                    <span class="task-tag">{m.pipeline_tag || 'text-generation'}</span>
                  </td>
                  <td class="model-stat-cell">❤️ {m.likes}</td>
                  <td class="model-stat-cell">⬇️ {m.downloads}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      ) : (
        <p class="no-data">Model data syncs daily via GitHub Actions. Check back soon!</p>
      )}
      <div class="cta-link">
        <a href="/slmhub/docs/models/generated/directory/">View All Models →</a>
      </div>
    </section>

  </div>
</StarlightPage>

<style>
  /* ============================================
     2026 Premium Homepage Styles
     ============================================ */

  /* Keyframe Animations */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(2deg); }
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.7; }
  }

  @keyframes gradientShift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  @keyframes blobMorph {
    0%, 100% { border-radius: 40% 60% 70% 30% / 40% 50% 60% 50%; }
    50% { border-radius: 54% 46% 38% 62% / 49% 70% 30% 51%; }
  }

  /* Base Section Styles */
  .homepage {
    padding-bottom: 4rem;
    position: relative;
    overflow-x: hidden;
  }

  .section-title {
    font-size: 2.2rem;
    font-weight: 700;
    text-align: center;
    margin-bottom: 0.75rem;
    font-family: 'Space Grotesk', system-ui, sans-serif;
    animation: fadeInUp 0.6s ease-out;
  }

  .section-subtitle {
    text-align: center;
    color: var(--sl-color-gray-3);
    margin-bottom: 3rem;
    font-size: 1.1rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    animation: fadeInUp 0.6s ease-out 0.1s both;
  }

  /* ============================================
     Hero Section
     ============================================ */
  .hero {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
    padding: 5rem 0 4rem;
    min-height: 60vh;
    position: relative;
  }

  /* Organic blob backgrounds */
  .hero::before {
    content: '';
    position: absolute;
    top: -20%;
    right: -15%;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(255, 107, 53, 0.15), transparent 70%);
    animation: blobMorph 10s ease-in-out infinite, float 12s ease-in-out infinite;
    z-index: 0;
    pointer-events: none;
  }

  .hero::after {
    content: '';
    position: absolute;
    bottom: -20%;
    left: -10%;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(74, 144, 226, 0.1), transparent 70%);
    animation: blobMorph 12s ease-in-out infinite reverse, float 15s ease-in-out infinite reverse;
    z-index: 0;
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    animation: fadeInUp 0.8s ease-out;
  }

  .hero-title {
    font-size: 3.8rem;
    font-weight: 800;
    line-height: 1.05;
    margin-bottom: 1.5rem;
    font-family: 'Space Grotesk', system-ui, sans-serif;
    letter-spacing: -0.02em;
  }

  /* Animated Gradient Text */
  .gradient-text {
    background: linear-gradient(135deg, #FF6B35, #FF9F7A, #8B5CF6, #FF6B35);
    background-size: 300% 300%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradientShift 5s ease infinite;
  }

  .hero-tagline {
    font-size: 1.25rem;
    margin-bottom: 2.5rem;
    line-height: 1.7;
    color: var(--sl-color-gray-2);
    max-width: 500px;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-image {
    position: relative;
    z-index: 1;
    display: flex;
    justify-content: center;
    align-items: center;
    animation: fadeInUp 0.8s ease-out 0.2s both;
  }

  .hero-image img {
    max-width: 320px;
    width: 100%;
    height: auto;
    filter: drop-shadow(0 30px 60px rgba(255, 107, 53, 0.25));
    animation: float 6s ease-in-out infinite;
  }

  /* ============================================
     Courses Section - Card Layout
     ============================================ */
  .courses-section {
    padding: 5rem 0;
    position: relative;
  }

  .courses-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    animation: fadeInUp 0.6s ease-out 0.2s both;
  }

  .course-card {
    background: linear-gradient(145deg, rgba(255,255,255,0.04) 0%, rgba(255,255,255,0.01) 100%);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 1rem;
    padding: 1.5rem;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .course-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    opacity: 0.7;
    transition: opacity 0.3s;
  }

  .course-card:hover {
    transform: translateY(-4px);
    border-color: rgba(255,255,255,0.2);
    box-shadow: 0 12px 40px rgba(0,0,0,0.3);
  }

  .course-card:hover::before {
    opacity: 1;
  }

  /* Card color variants */
  .card-orange::before { background: linear-gradient(90deg, #FF6B35, #FFA07A); }
  .card-purple::before { background: linear-gradient(90deg, #8b5cf6, #a78bfa); }
  .card-teal::before { background: linear-gradient(90deg, #14b8a6, #5eead4); }
  .card-blue::before { background: linear-gradient(90deg, #4A90E2, #85C0FF); }
  .card-gradient::before { background: linear-gradient(90deg, #FF6B35, #8b5cf6); }
  .card-dark::before { background: linear-gradient(90deg, #64748b, #94a3b8); }

  .card-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
  }

  .card-icon {
    font-size: 1.5rem;
  }

  .card-header h3 {
    font-size: 1.1rem;
    font-weight: 700;
    margin: 0;
    color: var(--sl-color-white);
    font-family: 'Space Grotesk', sans-serif;
  }

  .card-desc {
    color: var(--sl-color-gray-4);
    font-size: 0.85rem;
    margin: 0 0 1rem 0;
    line-height: 1.5;
  }

  .course-list {
    list-style: none;
    padding: 0;
    margin: 0 0 1rem 0;
  }

  .course-list li {
    margin-bottom: 0.4rem;
  }

  .course-list a {
    color: var(--sl-color-gray-3);
    text-decoration: none;
    font-size: 0.875rem;
    transition: all 0.2s;
    display: block;
    padding: 0.3rem 0;
  }

  .course-list a:hover {
    color: var(--sl-color-text-accent);
    padding-left: 0.5rem;
  }

  .card-link {
    display: inline-block;
    color: var(--sl-color-text-accent);
    font-size: 0.85rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s;
    margin-top: 0.5rem;
  }

  .card-link:hover {
    transform: translateX(4px);
  }

  /* ============================================
     Models Table Section
     ============================================ */
  .models-section {
    padding: 5rem 0;
    position: relative;
  }

  .models-table-wrapper {
    border-radius: 1rem;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(15, 23, 42, 0.6);
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.2);
    animation: fadeInUp 0.6s ease-out 0.2s both;
  }

  .models-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.9rem;
  }

  .models-table thead {
    background: linear-gradient(135deg, rgba(255, 107, 53, 0.15) 0%, rgba(139, 92, 246, 0.1) 100%);
  }

  .models-table th {
    padding: 1rem 1.25rem;
    text-align: left;
    font-weight: 700;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--sl-color-white);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .models-table td {
    padding: 0.9rem 1.25rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    color: var(--sl-color-gray-2);
  }

  .models-table tbody tr {
    transition: all 0.2s ease;
  }

  .models-table tbody tr:hover {
    background: rgba(255, 107, 53, 0.08);
  }

  .models-table tbody tr:last-child td {
    border-bottom: none;
  }

  .model-name-cell a {
    color: var(--sl-color-text-accent);
    text-decoration: none;
    font-weight: 600;
    transition: color 0.2s;
  }

  .model-name-cell a:hover {
    color: var(--sl-color-accent-high);
    text-decoration: underline;
  }

  .model-author-cell {
    color: var(--sl-color-gray-4);
    font-size: 0.85rem;
  }

  .task-tag {
    display: inline-block;
    background: rgba(255, 107, 53, 0.15);
    color: var(--sl-color-accent);
    font-size: 0.7rem;
    font-weight: 600;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    text-transform: uppercase;
  }

  .model-stat-cell {
    font-size: 0.85rem;
    white-space: nowrap;
  }

  .cta-link {
    text-align: center;
    margin-top: 2.5rem;
    animation: fadeInUp 0.6s ease-out 0.3s both;
  }

  .cta-link a {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--sl-color-text-accent);
    font-weight: 600;
    text-decoration: none;
    padding: 0.75rem 1.5rem;
    border: 2px solid var(--sl-color-accent);
    border-radius: 999px;
    transition: all 0.3s;
  }

  .cta-link a:hover {
    background: var(--sl-color-accent);
    color: white;
    transform: scale(1.05);
  }

  .no-data {
    text-align: center;
    color: var(--sl-color-gray-4);
    font-style: italic;
    padding: 3rem;
  }

  /* ============================================
     Responsive Design
     ============================================ */
  @media (max-width: 1024px) {
    .courses-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .hero {
      grid-template-columns: 1fr;
      text-align: center;
      padding: 3rem 0;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-tagline {
      font-size: 1.1rem;
      margin-left: auto;
      margin-right: auto;
    }

    .hero-actions {
      justify-content: center;
    }

    .hero-image img {
      max-width: 200px;
    }

    .hero::before, .hero::after {
      width: 250px;
      height: 250px;
    }

    .courses-grid {
      grid-template-columns: 1fr;
    }

    .models-table-wrapper {
      overflow-x: auto;
    }

    .models-table th,
    .models-table td {
      padding: 0.75rem 1rem;
      min-width: 100px;
    }

    .section-title {
      font-size: 1.8rem;
    }
  }
</style>
