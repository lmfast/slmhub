---
import { Card, CardGrid, Icon, LinkButton } from '@astrojs/starlight/components';
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import Footer from '../components/Footer.astro';
import fs from 'node:fs';
import path from 'node:path';

// Read model data from JSON (generated by sync_models.py via GitHub Actions)
let topModels: { display_name: string; author: string; likes: string; downloads: string; slug: string; pipeline_tag: string; model_size: string }[] = [];
let lastSyncedAt: string | null = null;
try {
  const modelsPath = path.resolve(process.cwd(), 'data/models.json');
  const modelsDataRaw = fs.readFileSync(modelsPath, 'utf-8');
  const modelsDataObj = JSON.parse(modelsDataRaw);
  
  // Handle both old format (array) and new format (object with models array)
  const modelsData = Array.isArray(modelsDataObj) ? modelsDataObj : (modelsDataObj.models || []);
  lastSyncedAt = modelsDataObj.last_synced_at || null;
  
  // Sort by likes (descending) and take top 8
  topModels = modelsData
    .sort((a: any, b: any) => {
      const likesA = parseInt(String(a.likes).replace(/[^\d]/g, '')) || 0;
      const likesB = parseInt(String(b.likes).replace(/[^\d]/g, '')) || 0;
      return likesB - likesA;
    })
    .slice(0, 8);
} catch (e) {
  // If file not found (e.g., first build), use empty array
  console.warn('models.json not found, using empty model list for homepage.');
}

const frontmatter = {
  title: 'SLM Hub',
  description: 'Developer-centric documentation for Small Language Models (SLMs). Learn, choose, and deploy locally or at the edge.',
  template: 'splash',
  prev: false,
  next: false,
};
---

<StarlightPage frontmatter={frontmatter} headings={[]}>
  <div class="sl-container homepage">
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-content">
        <h1 class="hero-title">
          Small Models. <span class="gradient-text">Big Impact.</span>
        </h1>
        <p class="hero-tagline">
          The open-source learning platform for Small Language Models. 
          <strong>100% executable</strong> — every guide runs in Google Colab.
        </p>
        <div class="hero-actions">
          <a href="/slmhub/docs/start-here/" class="button button-primary">
            <Icon name="rocket" />
            Start Learning
          </a>
          <a href="/slmhub/docs/models/generated/directory/" class="button button-secondary">
            <Icon name="star" />
            Browse Models
          </a>
        </div>
      </div>
      <div class="hero-image">
        <div class="hero-image-wrapper">
          <img src="/slmhub/logo.svg" alt="SLM Hub Logo" />
        </div>
      </div>
    </section>

    <!-- Courses Section - Redesigned with Cards -->
    <section class="courses-section">
      <div class="section-header">
        <h2 class="section-title">
          <Icon name="open-book" /> Courses
        </h2>
        <p class="section-subtitle">Everything you need to master Small Language Models — from first principles to production deployment.</p>
      </div>

      <CardGrid stagger>
        <!-- Fundamentals Card -->
        <Card title="Fundamentals" icon="information" class="course-card card-orange">
          <p class="card-desc">Core concepts every SLM developer needs to know</p>
          <ul class="course-list">
            <li><a href="/slmhub/docs/start-here/">Start Here: Introduction to SLMs</a></li>
            <li><a href="/slmhub/docs/learn/foundations/01-neural-networks-basics/">What is AI? Neural Networks</a></li>
            <li><a href="/slmhub/docs/learn/foundations/02-transformer-architecture/">Transformers & Attention</a></li>
            <li><a href="/slmhub/docs/learn/fundamentals/tokenization/">Tokenization (BPE, Subword)</a></li>
            <li><a href="/slmhub/docs/learn/fundamentals/quantization/">Quantization Methods</a></li>
          </ul>
          <a href="/slmhub/docs/learn/foundations/" class="card-link">View All Fundamentals →</a>
        </Card>

        <!-- Hands-On Card -->
        <Card title="Hands-On Tutorials" icon="laptop" class="course-card card-purple">
          <p class="card-desc">Practical guides with runnable code</p>
          <ul class="course-list">
            <li><a href="/slmhub/docs/learn/hands-on/01-first-slm-10min/">First SLM in 10 Minutes</a></li>
            <li><a href="/slmhub/docs/learn/hands-on/02-fine-tuning-lora/">Fine-Tuning with LoRA</a></li>
            <li><a href="/slmhub/docs/learn/hands-on/05-rag-system/">Building RAG Systems</a></li>
            <li><a href="/slmhub/docs/learn/hands-on/04-function-calling-agents/">Function-Calling Agents</a></li>
          </ul>
          <a href="/slmhub/docs/learn/hands-on/" class="card-link">All Tutorials →</a>
        </Card>

        <!-- Deployment Card -->
        <Card title="Deployment" icon="rocket" class="course-card card-teal">
          <p class="card-desc">Run models locally, in the cloud, or at the edge</p>
          <ul class="course-list">
            <li><a href="/slmhub/docs/deploy/quickstarts/ollama/">Ollama Quickstart (Local)</a></li>
            <li><a href="/slmhub/docs/deploy/quickstarts/vllm/">vLLM (High-Throughput)</a></li>
            <li><a href="/slmhub/docs/deploy/quickstarts/llama-cpp/">llama.cpp (CPU/Edge)</a></li>
            <li><a href="/slmhub/docs/deploy/quickstarts/transformers-js/">Transformers.js (Browser)</a></li>
          </ul>
          <a href="/slmhub/docs/deploy/" class="card-link">All Deployment Guides →</a>
        </Card>

        <!-- Tools & Resources Card -->
        <Card title="Tools & Resources" icon="setting" class="course-card card-blue">
          <p class="card-desc">Interactive tools and reference materials</p>
          <ul class="course-list">
            <li><a href="/slmhub/docs/tools/model-picker/">Interactive Model Picker</a></li>
            <li><a href="/slmhub/docs/tools/hardware-picker/">Hardware Calculator (VRAM)</a></li>
            <li><a href="/slmhub/docs/tools/resources/">Libraries & Frameworks</a></li>
            <li><a href="/slmhub/docs/models/generated/directory/">Model Directory</a></li>
          </ul>
          <a href="/slmhub/docs/tools/" class="card-link">All Tools →</a>
        </Card>

        <!-- Projects Card -->
        <Card title="Projects" icon="smiley" class="course-card card-gradient">
          <p class="card-desc">Build real applications with SLMs</p>
          <ul class="course-list">
            <li><a href="/slmhub/docs/learn/projects/01-code-assistant/">Build a Code Assistant</a></li>
            <li><a href="/slmhub/docs/learn/projects/02-personal-knowledge-base/">Personal Knowledge Base (RAG)</a></li>
            <li><a href="/slmhub/docs/learn/projects/03-function-calling-agent/">Function-Calling Agent</a></li>
          </ul>
          <a href="/slmhub/docs/learn/projects/" class="card-link">Start Building →</a>
        </Card>

        <!-- Advanced Card -->
        <Card title="Advanced & Research" icon="forward-slash" class="course-card card-dark">
          <p class="card-desc">Cutting-edge techniques and architectures</p>
          <ul class="course-list">
            <li><a href="/slmhub/docs/learn/advanced-topics/">Advanced Topics</a></li>
            <li><a href="/slmhub/docs/learn/cutting-edge/01-bitnet-quantization/">BitNet (1.58-bit)</a></li>
            <li><a href="/slmhub/docs/learn/advanced-architectures/02-mamba-architecture/">Mamba Architecture</a></li>
            <li><a href="/slmhub/docs/learn/mathematics/">Mathematics Deep-Dives</a></li>
          </ul>
          <a href="/slmhub/docs/learn/cutting-edge/" class="card-link">Explore Research →</a>
        </Card>
      </CardGrid>
    </section>

    <!-- Top Models Section - Table Format -->
    <section class="models-section">
      <div class="section-header">
        <h2 class="section-title">
          <Icon name="star" /> Popular Models
        </h2>
        <p class="section-subtitle">
          Auto-synced from Hugging Face daily. Sorted by community popularity.
          {lastSyncedAt && (
            <span class="sync-time">Last updated: {new Date(lastSyncedAt).toLocaleString()}</span>
          )}
        </p>
      </div>
      
      {topModels.length > 0 ? (
        <div class="models-table-wrapper">
          <table class="models-table" id="modelsTable">
            <thead>
              <tr>
                <th class="sortable" data-column="name">
                  <span class="th-content">
                    <Icon name="list" size="1em" />
                    Model
                  </span>
                </th>
                <th class="sortable" data-column="task">
                  <span class="th-content">
                    <Icon name="setting" size="1em" />
                    Task
                  </span>
                </th>
                <th class="sortable" data-column="size">
                  <span class="th-content">
                    <Icon name="file" size="1em" />
                    Size
                  </span>
                </th>
                <th class="sortable" data-column="likes">
                  <span class="th-content">
                    <Icon name="heart" size="1em" />
                    Likes
                  </span>
                </th>
                <th class="sortable" data-column="downloads">
                  <span class="th-content">
                    <Icon name="download" size="1em" />
                    Downloads
                  </span>
                </th>
              </tr>
            </thead>
            <tbody>
              {topModels.map((m) => {
                // Truncate model name if too long
                const displayName = m.display_name.length > 40 
                  ? m.display_name.substring(0, 37) + '...' 
                  : m.display_name;
                return (
                <tr data-name={m.display_name} data-author={m.author} data-task={m.pipeline_tag || 'text-generation'} 
                    data-size={m.model_size || '—'} 
                    data-likes={parseInt(String(m.likes).replace(/[^\d]/g, '')) || 0}
                    data-downloads={parseInt(String(m.downloads).replace(/[^\d]/g, '')) || 0}>
                  <td class="model-name-cell">
                    <a href={`/slmhub/docs/models/generated/${m.slug}/`} title={m.display_name}>{displayName}</a>
                    <span class="model-author">by {m.author}</span>
                  </td>
                  <td class="model-task-cell">
                    <span class="task-tag">{m.pipeline_tag || 'text-generation'}</span>
                  </td>
                  <td class="model-size-cell">{m.model_size || '—'}</td>
                  <td class="model-stat-cell">
                    <Icon name="heart" size="1em" /> <span class="stat-value">{m.likes}</span>
                  </td>
                  <td class="model-stat-cell">
                    <Icon name="download" size="1em" /> <span class="stat-value">{m.downloads}</span>
                  </td>
                </tr>
              )})}
            </tbody>
          </table>
        </div>
      ) : (
        <div class="no-data">
          <p>Model data syncs daily via GitHub Actions.</p>
          {lastSyncedAt ? (
            <p class="sync-info">Last sync: <strong>{new Date(lastSyncedAt).toLocaleString()}</strong></p>
          ) : (
            <p class="sync-info">Data will be available after the first sync runs.</p>
          )}
        </div>
      )}
      <div class="cta-section">
        <a href="/slmhub/docs/models/generated/directory/" class="button button-minimal">
          <Icon name="external" size="1em" />
          View All Models
        </a>
      </div>
    </section>

  </div>
</StarlightPage>

<style is:global>
  /* ============================================
     2026 Premium Homepage Styles
     ============================================ */

  /* Keyframe Animations */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(40px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px) rotate(0deg) scale(1); }
    50% { transform: translateY(-24px) rotate(3deg) scale(1.02); }
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.75; }
  }

  @keyframes shimmer {
    0% { background-position: -1000px 0; }
    100% { background-position: 1000px 0; }
  }

  @keyframes gradientShift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  @keyframes blobMorph {
    0%, 100% { border-radius: 40% 60% 70% 30% / 40% 50% 60% 50%; }
    50% { border-radius: 54% 46% 38% 62% / 49% 70% 30% 51%; }
  }

  @keyframes buttonGlow {
    0% { box-shadow: 0 0 20px rgba(255, 107, 53, 0.4); }
    50% { box-shadow: 0 0 30px rgba(255, 107, 53, 0.6); }
    100% { box-shadow: 0 0 20px rgba(255, 107, 53, 0.4); }
  }

  @keyframes scaleUp {
    from {
      opacity: 0;
      transform: scale(0.95);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  /* Base Section Styles */
  .homepage {
    padding-bottom: 6rem;
    position: relative;
    overflow-x: hidden;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
    animation: fadeInUp 0.7s ease-out 0.2s both;
  }

  .section-title {
    font-size: 2.2rem;
    font-weight: 800;
    text-align: center;
    margin-bottom: 0;
    font-family: 'Space Grotesk', system-ui, sans-serif;
    letter-spacing: -0.02em;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
  }

  .section-title :global(svg) {
    width: 2rem;
    height: 2rem;
    color: var(--sl-color-accent);
  }

  .section-subtitle {
    text-align: center;
    color: var(--sl-color-gray-3);
    margin-bottom: 0;
    font-size: 1rem;
    max-width: 650px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.6;
  }

  /* ============================================
     Hero Section
     ============================================ */
  .hero {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
    padding: 5rem 0 4rem;
    min-height: 60vh;
    position: relative;
  }

  /* Organic blob backgrounds */
  .hero::before {
    content: '';
    position: absolute;
    top: -20%;
    right: -15%;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(255, 107, 53, 0.15), transparent 70%);
    animation: blobMorph 10s ease-in-out infinite, float 12s ease-in-out infinite;
    z-index: 0;
    pointer-events: none;
  }

  .hero::after {
    content: '';
    position: absolute;
    bottom: -20%;
    left: -10%;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(74, 144, 226, 0.1), transparent 70%);
    animation: blobMorph 12s ease-in-out infinite reverse, float 15s ease-in-out infinite reverse;
    z-index: 0;
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    animation: fadeInUp 0.8s ease-out;
  }

  .hero-title {
    font-size: 3.8rem;
    font-weight: 800;
    line-height: 1.05;
    margin-bottom: 1.5rem;
    font-family: 'Space Grotesk', system-ui, sans-serif;
    letter-spacing: -0.02em;
  }

  /* Animated Gradient Text */
  .gradient-text {
    background: linear-gradient(135deg, #FF6B35, #FF9F7A, #8B5CF6, #FF6B35);
    background-size: 300% 300%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradientShift 5s ease infinite;
  }

  .hero-tagline {
    font-size: 1.25rem;
    margin-bottom: 2.5rem;
    line-height: 1.7;
    color: var(--sl-color-gray-2);
    max-width: 500px;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-top: 2.5rem;
  }

  /* Beautiful Animated Buttons */
  .button {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.85rem 2rem;
    border-radius: 0.75rem;
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .button :global(svg) {
    width: 1.2em;
    height: 1.2em;
    transition: transform 0.3s ease;
  }

  .button-primary {
    background: linear-gradient(135deg, #FF6B35 0%, #FF8C5A 100%);
    color: white;
    box-shadow: 0 4px 24px rgba(255, 107, 53, 0.3);
    border: 2px solid transparent;
  }

  .button-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 48px rgba(255, 107, 53, 0.5);
    animation: buttonGlow 2s ease-in-out;
  }

  .button-primary:hover :global(svg) {
    transform: scale(1.15) rotate(5deg);
  }

  .button-secondary {
    background: rgba(255, 107, 53, 0.1);
    color: var(--sl-color-accent);
    border: 2px solid var(--sl-color-accent);
  }

  .button-secondary:hover {
    background: var(--sl-color-accent);
    color: white;
    transform: translateY(-2px);
    box-shadow: 0 8px 32px rgba(255, 107, 53, 0.3);
  }

  .button-secondary:hover :global(svg) {
    transform: scale(1.2);
  }

  .button-minimal {
    background: transparent;
    color: var(--sl-color-accent);
    border: 2px solid var(--sl-color-accent);
  }

  .button-minimal:hover {
    background: var(--sl-color-accent);
    color: white;
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(255, 107, 53, 0.25);
  }

  .hero-image {
    position: relative;
    z-index: 1;
    display: flex;
    justify-content: center;
    align-items: center;
    animation: slideInRight 0.8s ease-out 0.2s both;
  }

  .hero-image-wrapper {
    position: relative;
    width: 100%;
    max-width: 380px;
    aspect-ratio: 1;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-image img {
    max-width: 100%;
    width: 100%;
    height: auto;
    filter: drop-shadow(0 30px 60px rgba(255, 107, 53, 0.25));
    animation: float 6s ease-in-out infinite;
    object-fit: contain;
  }

  /* ============================================
     Courses Section - Card Layout
     ============================================ */
  .courses-section {
    padding: 5rem 0;
    position: relative;
  }

  .courses-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    animation: fadeInUp 0.6s ease-out 0.2s both;
  }

  /* Starlight Card enhancements for course cards */
  .course-card {
    position: relative;
    overflow: visible;
  }

  .course-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    opacity: 0.7;
    transition: opacity 0.3s;
    border-radius: 1rem 1rem 0 0;
  }

  .course-card:hover::before {
    opacity: 1;
  }

  /* Card color variants */
  .card-orange::before { background: linear-gradient(90deg, #FF6B35, #FFA07A); }
  .card-purple::before { background: linear-gradient(90deg, #8b5cf6, #a78bfa); }
  .card-teal::before { background: linear-gradient(90deg, #14b8a6, #5eead4); }
  .card-blue::before { background: linear-gradient(90deg, #4A90E2, #85C0FF); }
  .card-gradient::before { background: linear-gradient(90deg, #FF6B35, #8b5cf6); }
  .card-dark::before { background: linear-gradient(90deg, #64748b, #94a3b8); }

  .card-desc {
    color: var(--sl-color-gray-4);
    font-size: 0.85rem;
    margin: 0 0 1rem 0;
    line-height: 1.5;
  }

  .course-list {
    list-style: none;
    padding: 0;
    margin: 0 0 1rem 0;
  }

  .course-list li {
    margin-bottom: 0.4rem;
  }

  .course-list a {
    color: var(--sl-color-gray-3);
    text-decoration: none;
    font-size: 0.875rem;
    transition: all 0.2s;
    display: block;
    padding: 0.3rem 0;
  }

  .course-list a:hover {
    color: var(--sl-color-text-accent);
    padding-left: 0.5rem;
  }

  .card-link {
    display: inline-block;
    color: var(--sl-color-text-accent);
    font-size: 0.85rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s;
    margin-top: 0.5rem;
  }

  .card-link:hover {
    transform: translateX(4px);
  }

  /* ============================================
     Models Table Section
     ============================================ */
  .models-section {
    padding: 5rem 0;
    position: relative;
  }

  .models-table-wrapper {
    border-radius: 1rem;
    overflow: hidden;
    border: 1px solid rgba(255, 107, 53, 0.2);
    background: linear-gradient(135deg, rgba(255, 107, 53, 0.05) 0%, rgba(139, 92, 246, 0.03) 100%);
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.1), 0 2px 8px rgba(255, 107, 53, 0.1);
    animation: fadeInUp 0.6s ease-out 0.2s both;
    backdrop-filter: blur(12px);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .models-table-wrapper:hover {
    border-color: rgba(255, 107, 53, 0.3);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15), 0 4px 16px rgba(255, 107, 53, 0.15);
  }

  [data-theme="dark"] .models-table-wrapper {
    background: linear-gradient(135deg, rgba(255, 107, 53, 0.08) 0%, rgba(139, 92, 246, 0.05) 100%);
    border-color: rgba(255, 107, 53, 0.25);
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4), 0 2px 8px rgba(255, 107, 53, 0.15);
  }

  [data-theme="dark"] .models-table-wrapper:hover {
    border-color: rgba(255, 107, 53, 0.35);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.5), 0 4px 16px rgba(255, 107, 53, 0.2);
  }

  .models-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.9rem;
  }

  .models-table thead {
    background: linear-gradient(135deg, rgba(255, 107, 53, 0.18) 0%, rgba(139, 92, 246, 0.12) 100%);
    backdrop-filter: blur(8px);
  }

  .models-table th {
    padding: 1rem 1.25rem;
    text-align: left;
    font-weight: 700;
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--sl-color-white);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    cursor: pointer;
    user-select: none;
    transition: all 0.2s ease;
  }

  .models-table th.sortable:hover {
    background: linear-gradient(135deg, rgba(255, 107, 53, 0.25) 0%, rgba(139, 92, 246, 0.18) 100%);
    transform: translateY(-1px);
  }

  .th-content {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .th-content :global(svg) {
    width: 1rem;
    height: 1rem;
    opacity: 0.7;
  }

  .models-table td {
    padding: 0.9rem 1.25rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    color: var(--sl-color-gray-2);
  }

  .models-table tbody tr {
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .models-table tbody tr:hover {
    background: linear-gradient(90deg, rgba(255, 107, 53, 0.12) 0%, rgba(139, 92, 246, 0.08) 100%);
    transform: translateX(2px);
    box-shadow: inset 4px 0 0 rgba(255, 107, 53, 0.4);
  }

  .models-table tbody tr:last-child td {
    border-bottom: none;
  }

  .model-name-cell {
    max-width: 300px;
  }

  .model-name-cell a {
    color: var(--sl-color-text-accent);
    text-decoration: none;
    font-weight: 600;
    transition: color 0.2s;
    display: block;
  }

  .model-name-cell a:hover {
    color: var(--sl-color-accent-high);
    text-decoration: underline;
  }

  .model-author {
    display: block;
    color: var(--sl-color-gray-4);
    font-size: 0.8rem;
    margin-top: 0.25rem;
    font-weight: normal;
  }

  .model-size-cell {
    font-size: 0.85rem;
    color: var(--sl-color-gray-3);
  }

  .task-tag {
    display: inline-block;
    background: rgba(255, 107, 53, 0.15);
    color: var(--sl-color-accent);
    font-size: 0.7rem;
    font-weight: 600;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    text-transform: uppercase;
  }

  .model-stat-cell {
    font-size: 0.85rem;
    white-space: nowrap;
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }

  .model-stat-cell :global(svg) {
    width: 1rem;
    height: 1rem;
    flex-shrink: 0;
  }

  .cta-section {
    text-align: center;
    margin-top: 3rem;
    animation: fadeInUp 0.6s ease-out 0.3s both;
  }

  .cta-section .button {
    display: inline-flex;
  }

  .no-data {
    text-align: center;
    color: var(--sl-color-gray-4);
    padding: 3rem;
    background: rgba(0, 0, 0, 0.2);
    border-radius: 1rem;
    border: 1px dashed rgba(255, 255, 255, 0.1);
  }

  .no-data p {
    margin: 0.5rem 0;
  }

  .sync-info {
    margin-top: 1rem;
    font-size: 0.9rem;
  }

  .sync-time {
    display: block;
    margin-top: 0.5rem;
    font-size: 0.85rem;
    color: var(--sl-color-gray-4);
  }

  .stat-value {
    font-weight: 600;
  }

  .model-stat-cell :global(svg) {
    opacity: 0.7;
  }

  /* ============================================
     Responsive Design
     ============================================ */
  @media (max-width: 1200px) {
    .hero {
      gap: 2rem;
      padding: 4rem 0 3rem;
    }

    .hero-title {
      font-size: 3.2rem;
    }
  }

  @media (max-width: 1024px) {
    .section-title {
      font-size: 1.95rem;
    }

    .courses-section :global(.sl-card-grid) {
      grid-template-columns: repeat(2, 1fr);
    }

    .models-table th,
    .models-table td {
      padding: 0.85rem 1rem;
    }
  }

  @media (max-width: 768px) {
    .hero {
      grid-template-columns: 1fr;
      text-align: center;
      padding: 3rem 0 2rem;
      gap: 2rem;
    }

    .hero-content {
      order: 2;
    }

    .hero-image {
      order: 1;
    }

    .hero-title {
      font-size: 2.25rem;
      line-height: 1.1;
    }

    .hero-tagline {
      font-size: 1rem;
      margin: 1rem auto;
    }

    .hero-actions {
      justify-content: center;
      gap: 0.75rem;
      flex-direction: column;
      width: 100%;
    }

    .button {
      width: 100%;
      justify-content: center;
    }

    .hero-image-wrapper {
      max-width: 280px;
    }

    .hero::before, .hero::after {
      width: 200px;
      height: 200px;
    }

    .section-title {
      font-size: 1.75rem;
      gap: 0.5rem;
    }

    .section-title :global(svg) {
      width: 1.5rem;
      height: 1.5rem;
    }

    .section-subtitle {
      font-size: 0.95rem;
    }

    .section-header {
      margin-bottom: 2.5rem;
    }

    .courses-section :global(.sl-card-grid) {
      grid-template-columns: 1fr;
    }

    .models-table-wrapper {
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
    }

    .models-table {
      min-width: 600px;
    }

    .models-table th,
    .models-table td {
      padding: 0.75rem 0.85rem;
      font-size: 0.8rem;
    }

    .model-name-cell a {
      font-size: 0.85rem;
    }

    .task-tag {
      font-size: 0.65rem;
      padding: 0.2rem 0.4rem;
    }
  }

  @media (max-width: 480px) {
    .hero-title {
      font-size: 1.75rem;
    }

    .hero-tagline {
      font-size: 0.95rem;
    }

    .button {
      padding: 0.75rem 1.5rem;
      font-size: 0.9rem;
    }

    .button :global(svg) {
      width: 1em;
      height: 1em;
    }

    .section-title {
      font-size: 1.5rem;
      gap: 0.35rem;
    }

    .section-title :global(svg) {
      width: 1.25rem;
      height: 1.25rem;
    }

    .models-table {
      min-width: 500px;
    }

    .models-table th {
      font-size: 0.65rem;
      padding: 0.6rem 0.7rem;
    }

    .models-table td {
      padding: 0.6rem 0.7rem;
    }
  }
</style>

<script>
  // Table Sorting Functionality
  document.addEventListener('DOMContentLoaded', () => {
    const table = document.getElementById('modelsTable');
    if (!table) return;

    const headers = table.querySelectorAll('th.sortable');
    let sortState = {};

    headers.forEach(header => {
      const column = header.getAttribute('data-column');
      sortState[column] = { direction: 'asc', active: false };

      header.addEventListener('click', () => {
        const tbody = table.querySelector('tbody');
        const rows = Array.from(tbody.querySelectorAll('tr'));

        // Toggle sort direction
        const isAsc = sortState[column].direction === 'asc';
        sortState[column].direction = isAsc ? 'desc' : 'asc';

        // Update active state
        headers.forEach(h => h.style.opacity = h === header ? '1' : '0.6');

        // Sort rows
        rows.sort((a, b) => {
          let aValue = a.getAttribute(`data-${column}`);
          let bValue = b.getAttribute(`data-${column}`);

          // Try to parse as numbers for numeric columns
          if (column === 'likes' || column === 'downloads') {
            aValue = parseInt(aValue) || 0;
            bValue = parseInt(bValue) || 0;
            return sortState[column].direction === 'asc' ? aValue - bValue : bValue - aValue;
          }
          
          // Handle size column (parse GB/MB etc)
          if (column === 'size') {
            const parseSize = (val: string) => {
              if (!val || val === '—') return 0;
              const num = parseFloat(val);
              if (val.includes('TB')) return num * 1000;
              if (val.includes('GB')) return num;
              if (val.includes('MB')) return num / 1000;
              if (val.includes('KB')) return num / 1000000;
              return num / 1000000000;
            };
            aValue = parseSize(aValue);
            bValue = parseSize(bValue);
            return sortState[column].direction === 'asc' ? aValue - bValue : bValue - aValue;
          }

          // String comparison
          return sortState[column].direction === 'asc'
            ? aValue.localeCompare(bValue)
            : bValue.localeCompare(aValue);
        });

        // Re-append sorted rows
        rows.forEach(row => tbody.appendChild(row));
      });
    });
  });
</script>
