---
---

<div class="site-title-wrapper">
  <a href="/slmhub/" class="site-title">
    <!-- Using public logo directly for reliability -->
    <img class="logo-img" src="/slmhub/logo.svg" alt="SLM Hub" width="28" height="28" />
    <span class="title-text">SLM Hub</span>
  </a>

  <nav class="navbar-links">
    <a href="/slmhub/docs/start-here/" class="nav-link">Course</a>
    <a href="/slmhub/docs/learn/fundamentals/" class="nav-link">Foundations</a>
    <a href="/slmhub/docs/models/generated/directory/" class="nav-link">Models</a>
    <a href="/slmhub/docs/community/" class="nav-link">Community</a>
    <a href="/slmhub/docs/about/" class="nav-link">About</a>
    <a href="/slmhub/docs/community/contributing/" class="nav-link">Contributing</a>
  </nav>
</div>

<style>
  .site-title-wrapper {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .site-title {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    text-decoration: none;
    color: var(--sl-color-text-accent);
    font-weight: 600;
    font-size: 1.25rem;
    white-space: nowrap;
  }

  .logo-img {
    height: 28px;
    width: 28px;
    color: var(--sl-color-text-accent);
    /* Ensure SVG inherits color if inline, but for img tag we rely on the file content unless we inline. 
       The SVG has 'currentColor' set, but <img> tags don't pass CSS color to SVGs. 
       However, the new logo has explicit gradients/colors now or currentColor? 
       The last edit used currentColor. For <img> tag, currentColor is black.
       Let's stick to the colored version or accept black/white. 
       Actually, standard Starlight renders pure SVG inline for this reason.
       But for robustness against [object Object], <img> is safer. 
       Let's assume the public logo has a default color or user is fine with it. */
  }

  .title-text {
    display: none; /* Hide text on small screens mostly, or keep it */
  }
  @media (min-width: 640px) {
    .title-text { display: block; }
  }

  /* Navbar Links */
  .navbar-links {
    display: none;
    gap: 1.5rem;
    align-items: center;
  }

  @media (min-width: 1024px) {
    .navbar-links {
      display: flex;
    }
  }

  .nav-link {
    text-decoration: none;
    color: var(--sl-color-text);
    font-size: 0.9rem;
    font-weight: 500;
    transition: color 0.2s;
  }

  .nav-link:hover {
    color: var(--sl-color-text-accent);
  }
</style>
